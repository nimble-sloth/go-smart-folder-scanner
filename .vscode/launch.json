{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Pre-built GUI (Fyne)",
            "type": "go",
            "request": "launch",
            "mode": "exec",
            "program": "${workspaceFolder}/my_gui.exe",
            "cwd": "${workspaceFolder}"
        }
    ]
}